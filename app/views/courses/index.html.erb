<h1>Courses</h1>
<% @courses.each do |course| %>
	<div class ="fillthisin">
		<h2><%= link_to course.title, course %></h2>
		<%= pluralize(course.levels.size, "Level") %>
	</div>
<% end %>

<p> <%= link_to 'New Course', new_course_path %></p>